# ==============================================
# GEMINI AI CONFIGURATION
# ==============================================
GEMINI_API_KEY=

# ==============================================
# CUSTOM SMTP CONFIGURATION
# ==============================================

# SMTP Host (mail server address)
SMTP_HOST=smtp.gmail.com

# SMTP Port
# - 587: STARTTLS (recommended, set SMTP_SECURE=false)
# - 465: SSL/TLS (set SMTP_SECURE=true)
# - 25: Plain (not recommended)
SMTP_PORT=465

# Use SSL/TLS encryption?
# - true for port 465 (SSL)
# - false for port 587 (STARTTLS)
SMTP_SECURE=true

# Your full email address
SMTP_USER=user@gmail.com

# Your email password or app-specific password
SMTP_PASS=AppPassword123

# Sender's email address (defaults to SMTP_USER if not set)
SENDER_EMAIL=user@gmail.com

# Sender's name (defaults to extracted profile name from resume if not set)
SENDER_NAME=John Doe